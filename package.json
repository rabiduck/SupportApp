{
  "name": "supportapp",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare-native SupportApp",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
